====================================================
  EMPLOYMENT PORTAL - MongoDB Collections Reference
====================================================

Database Name : employment_portal_db
MongoDB URI   : mongodb+srv://infolabztester:infolabztester@infolabz.nzobwfj.mongodb.net/employment_portal_db

----------------------------------------------------
  COLLECTIONS LIST
----------------------------------------------------

1. users
   - full_name
   - email
   - password
   - mobile_no
   - city
   - profile_image
   - role             → "User"
   - status           → "Active" / "Inactive" / "Blocked"
   - created_at

----------------------------------------------------

2. job_categories
   - category_name
   - category_description
   - category_image
   - status           → true / false

----------------------------------------------------

3. job_types
   - job_type_name    → e.g. "Full-Time", "Part-Time", "Contract"
   - description
   - status           → true / false

----------------------------------------------------

4. job_listings
   - category_id      → ObjectId (ref: job_categories)
   - job_type_id      → ObjectId (ref: job_types)
   - job_title
   - company_name
   - job_description
   - required_skills
   - salary_min
   - salary_max
   - job_location
   - created_at
   - status           → true / false

----------------------------------------------------

5. job_inquiries
   - user_id          → ObjectId (ref: users)
   - job_id           → ObjectId (ref: job_listings)
   - inquiry_message
   - inquiry_status   → "Pending" / "Responded"
   - inquiry_date
   - response_message
   - response_date

----------------------------------------------------

6. general_inquiries
   - user_id          → ObjectId (ref: users)
   - inquiry_subject
   - inquiry_message
   - inquiry_date
   - status           → "Pending" / "Closed"

----------------------------------------------------

7. feedbacks
   - user_id          → ObjectId (ref: users)
   - feedback_message
   - rating           → 1 to 5
   - feedback_date

====================================================
  TOTAL COLLECTIONS: 7
====================================================
